# Optional overrides when running from app/ (loads after root .env).
# Keep POSTGRES_PASSWORD in sync with the root .env and docker-compose.

DATABASE_URL=postgresql://postgres:monkeymanfix@localhost:5432/mini_it_platform
SECRET_KEY=change-me-to-a-long-random-string
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
